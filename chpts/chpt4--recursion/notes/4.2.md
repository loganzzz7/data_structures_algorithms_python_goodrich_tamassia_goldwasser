# 4.2 Analyzing Recursive Algorithms
- Binary Search:
    - `O(logn)` -> on each recursive call reduce problem size by half -> `logn` time
- Computing disk usage:
    - `O(n)` time -> `n` child paths called
