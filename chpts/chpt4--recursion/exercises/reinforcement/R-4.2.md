## Draw the recursion trace for the computation of power(2, 5)
```Python
def power(x, n):
    """compute the value of x^n for integer n"""
    if n == 0:
        return 1
    else:
        return x * power(x, n - 1)
```
- Drawing--...intuitive to `power(2, 5)`:
![Alt Text](/chpts/chpt4--recursion/exercises/reinforcement/imgs/R-4.2.png)