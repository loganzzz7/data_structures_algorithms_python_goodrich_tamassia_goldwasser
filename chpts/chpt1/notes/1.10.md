# 1.10 Scopes and Namespaces
- skipping
- one note:
## First Class Objs
- instances of a type that can be assigned to an identifier, passed as a param or returned by a func
    - in python functions and classes are also treated as first-class objs
        - can do `scream = print` then `scream("hello world")`
